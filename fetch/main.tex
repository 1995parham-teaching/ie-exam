\documentclass[../main.tex]{subfiles}

\begin{document}

\section{پرتال امن}

\paragraph{}
دانشگاه قصد دارد سرویس جدیدی ارائه دهد.
این سرویس یک قسمت وب دارد که با استفاده از جاوا اسکریپت توسعه پیدا
کرده است و در مرورگر کاربر اجرا می‌شود.

\paragraph{}
دانشگاه قصد دارد از تابع \lr{fetch} برای دسترسی به پرتال دانشکده استفاده کند.
در این کاربرد با نام کاربری و رمز عبور \textbf{ادمین} پرتال تقاضا ارسال می‌شود و اطلاعات دانشجو برای نمایش گرفته می‌شود.
آیا از نظر شما استفاده از این شیوه امن می‌باشد؟ فرض کنید دانشگاه از بستر ارتباطی \lr{HTTPS} استفاده می‌کند.
توضیح دهید.

\paragraph{}
۴ نمره

\answer{
    \begin{itemize}
            \item{عدم بیان دسترسی کاربران به اطلاعات ادمین پرتال - از دست دادن ۲۰ درصد نمره}
            \item{امن بودن ارتباط ارتباطی با استفاده از تابع \lr{fetch} ندارد - از دست دادن ۳۰ درصد نمره}
    \end{itemize}
}


\end{document}
